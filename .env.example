"""Root-level entry point — sets PYTHONPATH and launches main app."""
import sys
import os

# Ensure project root is on sys.path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

# Import and run main app
from app.main import *
